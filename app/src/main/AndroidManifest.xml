<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="c.com.phoneauthfirebase">

    <uses-permission
        android:name="android.permission.GET_ACCOUNTS"
        android:maxSdkVersion="22" />

    <application
        android:name=".PhoneAuthApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".Register" />
        <activity android:name=".Group" />
        <activity android:name=".Item" />
        <activity android:name=".AddGroup" />
        <activity android:name=".AddItem" />
        <activity android:name=".UploadImage" />
        <activity android:name=".SignupOrLogin" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ChatUsersListActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".ViewOrdersPageActivity" />
        <activity
            android:name=".ShowOrdersList"
            android:label="@string/title_activity_show_orders_list"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>